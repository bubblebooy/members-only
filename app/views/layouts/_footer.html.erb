<footer class="footer">
  <% if  !current_user.nil? %>
    <div>
      Current User:
      <%=  current_user.name%>
    </div>
    <%= link_to "Logout", logout_path, method: :delete %>
  <% else %>
    <%= link_to "Login", login_path%>
  <% end %>
  <%= link_to "All Posts", root_path%>
</footer>
